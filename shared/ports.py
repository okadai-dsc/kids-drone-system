"""通信ポート・IP の共通定義（同時稼働4台版）。

出典: docs/開発者補足/ポート一覧.md §3（提案版）。
※ 値は武田氏確認中の提案版。変更が来たらこのファイル 1 箇所で直す。
"""

# --- 台数 ---------------------------------------------------------------
NUM_DRONES = 4  # 同時稼働 4 台前提（8 台運用時は拡張する）

# --- IP アドレス割当 ----------------------------------------------------
# ノートPC（YOLO + 結果表示 + ドローンモニタ を集約）
NOTE_PC_IP = "192.168.0.100"

# Raspberry Pi #1〜#4
PI_IPS = {
    1: "192.168.0.11",
    2: "192.168.0.12",
    3: "192.168.0.13",
    4: "192.168.0.14",
}

# 各 Pi から見た Tello EDU の固定 IP
TELLO_IP = "192.168.10.1"

# --- Tello ローカル通信（Pi - Tello 間）--------------------------------
TELLO_CMD_PORT = 8889  # コマンド送信 (Pi → Tello)
TELLO_STATE_PORT = 8890  # ステータス受信 (Tello → Pi)。設計書 5.1 の 8990 は誤記
TELLO_VIDEO_PORT = 11111  # ビデオ受信 (Tello → Pi)

# --- Pi → YOLO PC ビデオ転送（4チャネル）------------------------------
PI_TO_YOLO_VIDEO_PORTS = {
    1: 11112,
    2: 11113,
    3: 11114,
    4: 11115,
}

# --- YOLO → 結果表示（4チャネル）--------------------------------------
YOLO_TO_DISPLAY_PORTS = {
    1: 11212,
    2: 11213,
    3: 11214,
    4: 11215,
}

# --- ビーコン集約（全 Pi → ドローンモニタ、1ポート共通宛先）----------
# 旧 11114（YOLO#3 ビデオと競合）を解消した値。
BEACON_PORT = 11231

# --- Scratch 拡張 → tellomon HTTP 制御 --------------------------------
SCRATCH_HTTP_PORT = 8001
